# `js-slack-bot` 🤖

## Local dev

### Prerequisite

- install [ngrok](https://ngrok.com/)

### Commands

- Slack cannot access the local dev server. Run `ngrok http http://localhost:3002` to forward requests, then copy the generated URL into your Slack app's Event Subscriptions configuration.
